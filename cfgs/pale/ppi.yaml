DEVICE: cuda

MODEL:
  NAME: pale
  
  EMBEDDING:
    NEG_SAMPLE_SIZE: 10 
    EMBEDDING_DIM: 300
    EMBEDDING_NAME: ''

    OPTIMIZER: Adam
    LR: 0.01
    BATCH_SIZE: 512
    EPOCHS: 1000

  MAPPING:
    MODE: single
    NUM_HIDDEN: 0
    ACTIVATE_FUNCTION: Null 
    LOSS_FUNCTION: euclidean
    TAU: 0.0
    BETA: 0.0
    TOP_K: False

    OPTIMIZER: Adam
    LR: 0.01
    BATCH_SIZE_TRAIN: 32
    BATCH_SIZE_VAL: 32
    EPOCHS: 100
    PATIENCE: 100
    VALIDATE: False